name: Base ➡️ Ubicación de Eventos
node: div
if: or address.url address.geo
id: address
pt: 0

boxes:
- if: address.url
  links:
  - type: address
    label: '{{ address }}'
    anchor: '{{ get address.url }}'
    btn: cta
- if: address.geo
  geos:
  - type: point
    geo: '{{ get address.geo }}'
