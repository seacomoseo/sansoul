# Remote sources are intentionally omitted so the copyable fixture builds offline.
# Add project-specific sources following the `data/remote.yml` CMS schema.

# - src: null # string *
#   csv: null # boolean
#   lang: null # [ es, en, fr, de, pt, it ] *
#   types:
#   - type: null # [ article, blog, event, product, brand, author, category, org, service, sections, modals... ]
#     index: null # string
#     maps:
#     - from: null # string
#       to: null # string *
#       def: null # string
#       kind: null # [ str, int, float, bool, slice, dict ]
#       sep: null # string
