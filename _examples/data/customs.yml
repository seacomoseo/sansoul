# Example of a custom object exposed in the global values editor.
- name: footer
  label: Pie de página
  widget: object
  sub:
  - var: size
  - var: color
  - var: fade
  - var: fade_to
  - var: bi
  - var: alpha
  - var: div
  - var: flip_x
  - var: flip_y
  - var: dots
  - var: md

# - types: [] # string list of types
#   pages: [] # string list of filenames
#   var: null # string get YAML variable
#   name: null # string
#   label: null # string
#   hint: null # string
#   widget: null # [ bool, int, float, str, txt, md, time, img, vid, vimg, select, radio, rel, slice, dict, hide ] *
#   def: null # string
#   i18n: null # [ null, true, duplicate ]
#   multi: null # boolean
#   opts: []
#   # - label: null # string
#   #   value: null # string
#   sub: [] # like root without `types` and `pages`
